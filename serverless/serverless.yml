service: dataeng-futures-wh

package:
  individually: true

provider:
  name: aws
  runtime: python3.7
  tracing:
    lambda: true
  environment:
    HANDLER: src/handler/get_futures.get_futures
    SCHEDULE: rate(3 hours)
    DYNAMODB_TABLE: ${self:service}-${opt:stage, self:provider.stage}-endpoint-hash-table
    BUCKET: ${self:service}-${opt:stage, self:provider.stage}
    XAPIKEY: 'XbiOHGfBxxvSmXBQdb6vF3gZ9P3DWUgHVmmrRpSe'
  stage: ${opt:stage, 'dev'}
  region: us-east-1
  timeout: 60

  iamRoleStatements:
    - Effect: Allow
      Action:
        - s3:PutObject
        - s3:GetObject
      Resource: 'arn:aws:s3:::${self:provider.environment.BUCKET}/*'
    - Effect: Allow
      Action:
        - dynamodb:Query
        - dynamodb:Scan
        - dynamodb:GetItem
        - dynamodb:PutItem
        - dynamodb:UpdateItem
        - dynamodb:DeleteItem
      Resource: 'arn:aws:dynamodb:${opt:region, self:provider.region}:*:table/${self:provider.environment.DYNAMODB_TABLE}'


functions:
  # NFL
  nfl:
    handler: ${self:provider.environment.HANDLER}
    environment:
      WH_API: 'https://odds.us.williamhill.com/api/v1/events?competitionId=007d7c61-07a7-4e18-bb40-15104b6eac92'
      ENDPOINT: 'nfl'
    events:
      - schedule: ${self:provider.environment.SCHEDULE}
  # NBA
  nba:
    handler: ${self:provider.environment.HANDLER}
    environment:
      WH_API: 'https://odds.us.williamhill.com/api/v1/events?competitionId=5806c896-4eec-4de1-874f-afed93114b8c'
      ENDPOINT: 'nba'
    events:
      - schedule: ${self:provider.environment.SCHEDULE}
  # MLB
  mlb:
    handler: ${self:provider.environment.HANDLER}
    environment:
      WH_API: 'https://odds.us.williamhill.com/api/v1/events?competitionId=04f90892-3afa-4e84-acce-5b89f151063d'
      ENDPOINT: 'mlb'
    events:
      - schedule: ${self:provider.environment.SCHEDULE}
  # NHL
  nhl:
    handler: ${self:provider.environment.HANDLER}
    environment:
      WH_API: 'https://odds.us.williamhill.com/api/v1/events?competitionId=b7b715a9-c7e8-4c47-af0a-77385b525e09'
      ENDPOINT: 'nhl'
    events:
      - schedule: ${self:provider.environment.SCHEDULE}
  # NCAAF
  ncaaf:
    handler: ${self:provider.environment.HANDLER}
    environment:
      WH_API: 'https://odds.us.williamhill.com/api/v1/events?competitionId=b7eda1b3-0170-4510-9616-1bce561d7aa7'
      ENDPOINT: 'ncaaf'
    events:
      - schedule: ${self:provider.environment.SCHEDULE}
  # NCAAB
  ncaab:
    handler: ${self:provider.environment.HANDLER}
    environment:
      WH_API: 'https://odds.us.williamhill.com/api/v1/events?competitionId=d246a1dd-72bf-45d1-bc86-efc519fa8e90'
      ENDPOINT: 'ncaab'
    events:
      - schedule: ${self:provider.environment.SCHEDULE}

resources:
  Resources:
    whBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: ${self:provider.environment.BUCKET}
    endpointHashTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.DYNAMODB_TABLE}
        AttributeDefinitions:
          -
            AttributeName: endpoint
            AttributeType: S
        KeySchema:
          -
            AttributeName: endpoint
            KeyType: HASH
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1
